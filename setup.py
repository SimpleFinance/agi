from setuptools import setup

meta = dict(
    name             = "eyewall",
    version          = "0.0.1",
    packages         = ["eyewall"],
    install_requires = [
        ],
    )

setup(**meta)
